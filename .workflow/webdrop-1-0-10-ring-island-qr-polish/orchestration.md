# Orchestration: WebDrop 1.0.10 ring island QR polish

## Execution Rules

- Keep the original objective intact.
- Ask for approval before risky, expensive, external, or destructive actions.
- Keep immediate blocking work local.
- Delegate only bounded, disjoint, materially useful packets.
- Integrate packet results before final verification.

## Branching Rules
- If ring restoration creates duplicate rings, prefer styling the image itself over reintroducing button pseudo-elements.
- If QR scanner title cannot fit at large text sizes, reduce only the scanner headline size, not the whole island layout.
- If close artifact persists, make the cancel control fade earlier than the island shell and verify the final state is visually inert.
- If tests/docs fail from version drift, update the source and generated deliverables together.

## Packet Prompts
- CSS visual audit: inspect `css/orbit.css`, `css/sheets.css`, `css/dynamic-island.css`, and screenshots for ring/candidate/dark-mode issues.
- JS behavior audit: inspect `js/ui/dynamic-island.js`, `js/ui/app-view.js`, `js/config/i18n.js`, and tests for QR fallback and close-state behavior.
- Release QA audit: inspect versioned docs/tests/scripts/output expectations after implementation and identify missed metadata.

## Completion Audit
- Record accepted/rejected subagent findings in `final-report.md`.
- Include exact verification commands and Vercel status.

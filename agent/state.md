# Project State

## Current state summary (≤200 words)
WebDrop v2 is at **version 1.0.94**. The physical-proximity path now uses a server-persisted runtime policy: score weights, minimum score, late-tap grace, acoustic window, and bump-time slop are editable from the admin **Settings** tab and snapshotted per session. Defaults are a 6,000 ms late-tap grace, 6,000 ms acoustic window, and 4,000 ms match slop. The admin **Test cases** tab assigns live devices to intended pairs, records bounded diagnostic evidence, and keeps local run history tied to the exact policy revision. Capacity remains 100 participants / up to 50 pairs globally, with a configured six-device cohort cap. The core static PWA, WebRTC transfer pipeline, storage ladder, English/Japanese UI, and Azure signaling backend remain intact.

At the start of this session the working tree was effectively clean — the only pre-existing change in git status was an untracked `.cursor/mcp.json`. No application source files were mid-edit. Unit (`node --test`) and Playwright e2e suites exist; notable gaps: no coverage gate, no dedicated test for the data-channel transfer protocol, and live/relay e2e specs require the external server (see `tests.md`).

This `agent/` knowledge base was generated today from the real code as the standardized agent context for the repo.

## Recent changes
- **2026-07-02 — Admin dashboard polish, mobile responsiveness, JP completeness, honest readiness (v1.0.94):** Shipped four operator-facing improvements ahead of the mobile test day. **(A) Diagnostics resilience:** `refreshDiagnostics` (`js/admin/readiness.js`) no longer lets a missing/invalid admin token make a healthy server look offline — `/readyz` + `/api/proximity-policy` (both public) now determine `serverReachable` independently of the token-gated `/api/diagnostics-public` snapshot. A tokenless viewer sees "Connected" plus a guidance banner (`adminTokenNeeded`) instead of a false "offline / 22%". The one-time `tokenPromptDismissed` guard is preserved (no per-poll prompt spam). This was the root cause of the reported "22% ready": tokenless = server-unreachable branch = 2/9. **(B) Honest readiness:** proximity ceremony moved from a flat "proof" item to a new **"partial"** status (icon ◐, label "1-on-1 verified") that counts toward `readinessSummary`; a healthy server now reads **56%** (5/9) reflecting proven 1-on-1 pairing, not a hardcoded number. **(C) Desktop Settings layout:** replaced the cramped 3-column `.tuning-form` (fieldset|fieldset|thin actions strip) with balanced 2-column fieldsets and a full-width action row (status left, Apply right); bumped sub-10px helper/label fonts to a ≥9.5px legible floor. **(D) Mobile responsiveness:** fixed the Test cases tab clipping — `.test-main` used an implicit `auto` grid column and its sections had `min-width:auto`, forcing 606px and overflow; now `grid-template-columns: minmax(0,1fr)` + `.test-main > section { min-width:0 }`. Verified no horizontal overflow at 375px across all four tabs. **(E) Japanese:** localized the 7-case test catalog (short/title/purpose/procedure via `TEST_CASE_I18N_JA` + `localizeCase`, English remains source of truth) and the two hardcoded detail list lines (`assignPairsTwo/One`, `keepConditionsNote`); added `statusPartial` + `adminTokenNeeded`. Bumped 1.0.93→**1.0.94** across all source, `?v=` suffixes, both `APP_VERSION` constants, `package.json`, and `package-lock.json`. `npm run verify:full` green (frontend 49/49, backend 54/54, 0 vulnerabilities, clean secret audit + `git diff --check`). Browser-verified desktop + 375px mobile in EN and JA with no console errors. Admin auth remains a single shared `METRICS_API_TOKEN` (exact-match in `server.js`); no server change — the operator shares the one existing token with helpers. See ADR-0010.
- **2026-07-02 — Pre-campaign production verification; physical run still pending (no code change):** Continued from `0c033a9`. Verified live production is ready for the multi-person proximity test campaign but **did not run it** — the campaign requires four physical phones bumped/tilted ~5 m apart with real over-air ultrasound, which cannot be performed or simulated by an agent without fabricating evidence. Verified: authenticated read of `GET /api/proximity-policy` on the Azure signaling VM returns **policy revision 2** (min score 55; weights sound 34 / motion 26 / bump 20 / tilt 12 / QR 8; late-tap grace 6000 ms; acoustic window 6000 ms; match slop 4000 ms; `updatedAt 2026-07-01T11:51:05Z`) — identical on public and `Authorization: Bearer` reads; `/readyz` healthy (`ok:true`, production, TURN configured, `turnAuthRequired:true`, proximity analysis enabled); Vercel frontend serving `1.0.93` (`?v=1.0.93`, service-worker `APP_VERSION 1.0.93`). Ran `npm run verify:full`: frontend **49/49**, backend **54/54**, secret audit clean, `npm audit` 0 vulnerabilities, `git diff --check` clean. Baseline policy revision 2 was **left unchanged** (one-variable-per-revision rule requires measuring the baseline before any tuning). `.cursor/mcp.json` preserved untracked. No source files modified; no ADR (no architectural decision). Remaining work is the human-operated over-air campaign: 10+ attempts per geometry (top-edge, speaker-to-mic, cross-angle), the 0/0.5/1/1.5/3/5 s delayed-tap sweep, two intended pairs ~5 m apart, and negative controls — then calibrate defaults from measured failures without weakening the ultrasound+bump+tilt gate.
- **2026-07-01 — Runtime proximity tuning + repeatable multi-device test console (v1.0.93):** Added `RuntimeProximityPolicy` with validation, atomic JSON persistence, authenticated `PUT /api/proximity-policy`, and session-level policy snapshots. Late partner admission now waits for the full configurable grace (default 6 s), then starts promptly once another device joins; acoustic duration and match slop come from the same snapshot. Client scoring and ceremony timing consume server tuning. Moved tuning to a dedicated admin **Settings** tab. Added a **Test cases** tab with seven researched scenarios, live device Pair A/B assignment, start/stop recording, policy revision capture, event de-duplication, correct/wrong-pair counts, evidence rates, medians, and local history. Added English/Japanese labels, responsive fixes, unit/backend/e2e coverage, and service-worker precache for `test-runs.js`. `npm run verify` is green (49/49), backend tests are green (54/54), and the targeted admin Playwright suite is green (5/5). Browser QA proved Settings, a start/stop run with four simulated devices, and 390 px layout without horizontal overflow. See ADR-0009.
- **2026-06-30 — Tiles-as-progress HUD, swipe polish, live profiles (v1.0.92):** Three fixes. **(A) Transfer HUD = the tiles.** Removed the separate gradient progress bar; the island tile-wave itself now visualizes progress. `TileWave` gained `setTransferMode(on)`/`setProgress(ratio)`; in the per-tile draw loop, tiles whose `(sweepCol+1)/cols > progress` render as a dim, desaturated "remaining track" (no glow) while filled tiles keep the animated sweep — so the lit region grows L→R (send) / R→L (receive). Honored in the reduced-motion/static path too (`#drawStatic` + `#repaintIfStatic`). `DynamicIsland.syncWave` toggles transfer mode; `paintTransferProgress` drives `wave.setProgress` and no longer writes the meter. Deleted `.webdrop-island__meter` (CSS + HTML), kept the wave full-opacity during transfer, and restyled `.webdrop-island__transfer` as a full-height flex column (percent/label top, filename bottom, tiles through the middle). **(B) Swipe-up-to-send polish** (`css/sheets.css`): softened the thumb's blue shadow, trimmed `.swipe-control--vertical` height (132→110px, maxDistance ≈48px) so there's no big empty blue runway, lightened the `::before` affordance, and lifted the "Swipe up to send" label cleanly above the thumb. Gesture math (`bindSwipe`) unchanged. **(C) Live profile propagation:** changing avatar/ring/name now broadcasts `{type:"profile:update", profile}` over the open data channel (new `WebRtcTransport.sendControlMessage` + forwarded `control` event; `DataChannelTransferProtocol.sendControl` reused, default-case `control` emit) so the connected peer's store record + connected-screen avatar + island peer avatar refresh live (`DynamicIsland.updatePeerProfile`). Best-effort presence re-announce for nearby devices via `WebSocketSignalingAdapter.updateProfile(self)` (re-sends `client:hello` with cached capabilities; `MockSignalingAdapter` no-ops). 250 ms debounce + change-detection avoid spam. Bumped to **1.0.92**; `npm run check` + 45 unit tests green. See ADR-0007, ADR-0008.
- **2026-06-30 — Dark-theme halo + stale-QR follow-up (v1.0.91):** Killed the *systemic* white-halo root cause: in dark theme `--ink` flips to near-white (`#f4f4f2`), so avatar `box-shadow: …color-mix(var(--ink) …)` rendered as a white glow. Added a theme-stable `--shadow-ink: #14141a` token (`css/base.css`) and pointed the avatar drop-shadows at it across `css/orbit.css` (`.peer-node` radar avatars, `.avatar--self`, `.peer-overlap`) and `css/sheets.css` (`.peer-candidate-avatar`, `.friend-strip`, `.nearby-device-avatar`, `.avatar-upload__button`, `.avatar-cropper__preview`) — light theme is visually identical, dark theme no longer glows. Also cleared the display canvas on entering `qr-scan` (`DynamicIsland.showQrScanner`) so a previously-shown QR no longer lingers behind the scan viewfinder (detection uses the separate offscreen `scanCanvas`, so this is safe). Bumped to **1.0.91**. `npm run verify` green (check + 45 tests). (Flagged during review by a parallel verification agent.)
- **2026-06-30 — UI polish + personalized QR (v1.0.90):** Fixed the white "halos": removed the blurred radial glow behind the island tile-wave (`.webdrop-island__flow::before` deleted, wave `drop-shadow` dropped) and the white disc behind avatar-carousel icons (`background:#fff`/shadow → transparent). **Personalized the "Show this code nearby" QR** — `DynamicIsland.drawQr(token, avatar)` now renders at error-correction **level H** and composites the user's avatar as a rounded centre badge (~24% of the code, over a white knockout); images are cached (`qrLogoCache`) and painted synchronously when warm, so there's no scan delay. Polished the scan viewfinder (refined corner brackets + gradient/glow beam + pill camera button). Added a **500 MB send/receive** info line to the send sheet (`transferSizeLimit`, en+ja). New `tests/qr-personalized.test.mjs` proves the badged level-H code still decodes via `jsQR` (geometry mirrors the runtime). Committed as `32b1fd9`, pushed to `origin/main`. See ADR-0006.
- **2026-06-29 — Diagram layout fix:** Rewrote `graph/architecture.d2` to use the **ELK** layout engine (config header `vars.d2-config.layout-engine: elk`, `pad: 40`, `center: true`, `direction: down`) and applied clean-layout rules — flow-ordered top-down containers, short single-line labels, `transfer-engine.js` placed adjacent to the WebRTC transport/protocol nodes (kills the long sweeping `sendFiles` edge), and the admin dashboard placed beside the Azure server it polls. Re-rendered `architecture.svg` with `d2 --layout elk --pad 40`; visual check (PNG) shows no connector crossing a box and no overlapping containers. Same nodes/edges as before — only the layout was restructured. See ADR-0005. No application code changed.
- **2026-06-29 — Bootstrap:** Created the `agent/` knowledge base (router + architecture/setup/api/components/data/conventions/tests/errors/secrets/decisions/state + `graph/` with dependency DOT/JSON, `architecture.d2`, and rendered `architecture.svg`). Added root pointer files `AGENTS.md`, `CLAUDE.md`, and Cursor rule `.cursor/rules/agent-folder.mdc`. No application code changed; changes left unstaged/uncommitted for review.

<!-- Append new dated entries above this line as the project evolves. Keep the summary current. -->

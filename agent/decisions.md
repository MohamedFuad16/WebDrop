# Architecture Decision Records (ADRs)

Append-only. Newest at the bottom. Format: ID · date · title · status · context · decision · consequences. These initial ADRs are **reverse-engineered** from the existing architecture to capture the rationale evident in the code.

---

## ADR-0001 · 2026-06-29 · Use WebRTC data channels for file transfer
**Status:** Accepted (in code)
**Context:** WebDrop transfers user files (up to 500 MB) between two devices that are physically near each other. Routing file bytes through a central server is costly, slow, and a privacy concern.
**Decision:** Send files **peer-to-peer over WebRTC `RTCDataChannel`** (`js/services/webrtc-transport.js` + `data-channel-transfer-protocol.js`). The WebSocket server is used only for the control plane (presence, invites, SDP/ICE relay, telemetry); it explicitly **rejects binary frames**. Two ordered channels separate control (`webdrop-control-v1`) from file bytes (`webdrop-file-v1`), with a manifest+chunk+ack protocol, SHA-256 integrity, backpressure, and retries.
**Consequences:** Direct/relay path depends on NAT and TURN availability (see `errors.md`); needs a TURN broker for symmetric NATs. Keeps bytes off the server and scales the data plane horizontally for free. Adds protocol complexity (ordering, retry, caps) the app must own.

---

## ADR-0002 · 2026-06-29 · Provide a mock signaling + simulated runtime fallback
**Status:** Accepted (in code)
**Context:** Real signaling, proximity (ultrasound/motion), and transfer require a deployed server, real hardware, and two devices — terrible for UI iteration and CI.
**Decision:** Define one signaling interface implemented by both `WebSocketSignalingAdapter` (production) and `MockSignalingAdapter` (in-memory, 15 fake peers, simulated invites/proximity/QR/RTC). Runtime flags (`runtime-flags.js`/`runtime-config.js`) select the adapter and gate `realProximityCeremony`/`realTransfer`/`qrPairing`; `?runtime=mock` forces mock on localhost.
**Consequences:** The entire UI runs offline with zero backend. Risk: developers may think they're hitting production when they're on mock (documented in `errors.md`). Mock and real paths must be kept behaviorally aligned.

---

## ADR-0003 · 2026-06-29 · Multi-factor physical-proximity verification (ultrasound + motion, QR fallback)
**Status:** Accepted (in code)
**Context:** "Nearby" must mean physically present, not merely on the same signaling server, to prevent remote impersonation/relay attacks during pairing.
**Decision:** Run a **ceremony** combining ultrasonic chirp exchange (`acoustic-proximity.js`), DeviceMotion bump+tilt (`motion-proximity.js`), and an optional QR token (`proximity-token.js`), scored by `proximityScore`. A pass requires score ≥ 55 **and** ultrasound+bump+tilt present (`hasRequiredPhysicalEvidence`), mirroring the authoritative server gate. QR pairing is the explicit fallback when sensors are unavailable.
**Consequences:** Strong proximity assurance; but hardware/permission/secure-context dependent and brittle on some devices (see `errors.md`). The client must not over-report success vs the server, hence the duplicated gate.

---

## ADR-0004 · 2026-06-29 · Ship as a no-build static PWA with a layered storage ladder
**Status:** Accepted (in code)
**Context:** The app targets mobile browsers (esp. iPhone) and must be cheap to host, installable, offline-capable, and able to save large received files despite divergent browser storage support.
**Decision:** No bundler/transpiler — plain ES modules served statically (`scripts/static-server.mjs`), versioned via `?v=<APP_VERSION>` and a precaching `service-worker.js`. Received files use a capability/size-selected backend ladder in `StorageClient`: OPFS/IndexedDB (deferred) → StreamSaver streaming → in-memory Blob (iOS, ≤128 MB).
**Consequences:** Zero build pipeline, trivial deploy, instant module reloads — but no tree-shaking/minification and manual cache-version discipline (bump in several places). Storage behavior differs by platform; iOS large transfers are capped.

---

## ADR-0005 · 2026-06-29 · Render the architecture diagram with D2's ELK engine
**Status:** Accepted
**Context:** `graph/architecture.d2` was rendered with D2's default `dagre` layout, which routed connectors straight through boxes and swept long curved edges across the canvas (notably the `transfer → transport.protocol` `sendFiles` edge), making the diagram hard to read.
**Decision:** Render with the **ELK** layout engine (orthogonal edge routing, avoids node overlap) — applied both in the source (`vars.d2-config.layout-engine: elk`, `pad: 40`, `center: true`, `direction: down`) and via the CLI (`d2 --layout elk --pad 40`). Adopt clean-layout authoring rules: containers ordered top-to-bottom in data-flow order, tightly-coupled nodes in the same container, hub node (`controller.js`) kept central, short single-line labels, no manual `near` constraints, and nodes repositioned (e.g. `transfer-engine.js` next to the transport nodes, admin next to Azure) rather than crossing the canvas with long edges. ELK is open-source and bundled with d2; the proprietary `tala` engine was rejected.
**Consequences:** The diagram is clean — no connector crosses a box, no containers overlap. The same change was baked into the `agent-folder` skill (render script + template + docs) so future diagrams render cleanly by default. Authors must keep `.d2` sources flow-ordered for ELK to produce the best layout.

---

## ADR-0006 · 2026-06-30 · Personalize the connection QR with an avatar badge (level H)
**Status:** Accepted (in code)
**Context:** The "Show this code nearby" QR was a generic coloured matrix. Product wanted it to feel personal (per-user) without hurting scannability or adding latency to the pairing ceremony.
**Decision:** Render the QR at **error-correction level "H"** (~30% recovery) instead of "M", then composite the user's avatar as a rounded centre badge sized to ~24% of the code over a white "knockout" quiet zone (`DynamicIsland.drawQr(token, avatar)`). Avatar images are decoded once and kept in a module-level `qrLogoCache`, so a warm badge paints synchronously in the same frame as the modules — no async gap before a scan. A node test (`tests/qr-personalized.test.mjs`) reproduces the exact geometry into an RGBA buffer and asserts `jsQR` still decodes the original token, so the personalization can never silently break pairing.
**Consequences:** The code is denser at level H (smaller cells for the same token) but still scans comfortably at the rendered size, and the centre logo is well within H's recovery budget. Any future change to badge size / knockout padding must keep the decode test green. The same `drawQr` path is used only for the *display* QR; the scanner path is unaffected.

---

## ADR-0007 · 2026-06-30 · The transfer HUD is the tile-wave itself (no separate bar)
**Status:** Accepted (in code)
**Context:** During a transfer the island showed an animated tile-wave dimmed to 30%/blurred behind a separate gradient progress bar (`.webdrop-island__meter`). It felt redundant and busy — two competing progress visuals on a tiny surface.
**Decision:** Make the tiles themselves the progress bar. `TileWave` gained `setTransferMode(on)` + `setProgress(ratio)`; in the draw loop each tile computes `colFill = (sweepCol + 1) / cols` (where `sweepCol` already mirrors for send vs receive via `setDirection`). When transfer mode is on and `colFill > progress` the tile renders as a dim, desaturated "remaining track" with no glow; filled tiles keep the normal animated sweep, so the lit region grows with progress while still shimmering. The static/reduced-motion path (`#drawStatic`) honors the same rule, and `setProgress`/`setTransferMode` trigger a redraw when the rAF loop isn't running (`#repaintIfStatic`). `DynamicIsland.paintTransferProgress` now calls `wave.setProgress` and only updates the percent text; the `.webdrop-island__meter` element + CSS were deleted and the wave stays full-opacity during transfer (`.webdrop-island__transfer` is a full-height flex column — percent/label top, filename bottom, tiles through the middle).
**Consequences:** One coherent progress visual; less DOM/CSS. The percentage text now sits over the tiles, so it carries a text-shadow for legibility. Removing the bar required updating the `app-ui` e2e (it asserted on `[data-island-transfer-bar]`); the `data-island-transfer-percent/label/name` hooks are unchanged.

---

## ADR-0008 · 2026-06-30 · Propagate live profile changes over the data channel (+ best-effort presence)
**Status:** Accepted (in code)
**Context:** Changing avatar/ring/name only mutated local `state.self`; an already-connected peer (and nearby devices) kept showing the stale icon/name until reconnect.
**Decision:** Reuse the existing control plane. The connected case is reliable: the controller broadcasts `{ type: "profile:update", profile: { name, avatar, avatarId, ringColor } }` over the open WebRTC **control** channel via a new `WebRtcTransport.sendControlMessage()` → `DataChannelTransferProtocol.sendControl()`; the protocol's `default` switch case already emits a `control` event, which the transport now forwards. The controller validates the incoming profile (`isAllowedAvatarChoice`, `#ringColor` regex, 22-char name), patches the connected peer in the store (re-renders orbit + connected screen), and calls `DynamicIsland.updatePeerProfile()` to refresh the island peer avatar/name live (the store-driven `sync` can't, since the peer id is unchanged). The nearby case is best-effort: `WebSocketSignalingAdapter.updateProfile(self)` re-sends `client:hello` with the cached `capabilities`, no-opping when the socket is closed (`MockSignalingAdapter` just updates its cached self). Sends are debounced 250 ms with change-detection so name typing doesn't spam.
**Consequences:** Connected peers update instantly without reconnect; nearby refresh depends on whether the (external) relay rebroadcasts the re-announce. Custom (data-URL) avatars are allowed through and travel over the ordered control channel — acceptable for the small 256² crops, but very large data URLs would be a heavier control message.

---

## ADR-0009 · 2026-07-01 · Persist and snapshot runtime proximity policy; record tests against revisions
**Status:** Accepted (in code)
**Context:** Real users rarely tap Connect in the same instant. The prior effective admission window was only about 3.6 seconds (1.8-second join window plus one extension), while ceremony timing and scoring were spread across environment variables and constants. Tomorrow's multi-person test also needs reproducible evidence: changing a threshold halfway through a session, or losing which values produced a result, would make calibration unreliable.
**Decision:** Make one validated `RuntimeProximityPolicy` authoritative for score weights, minimum score, late-tap grace, acoustic window, and match slop. Persist it atomically on the signaling server, expose it through authenticated `GET`/`PUT /api/proximity-policy`, and snapshot the current revision onto every new session. A lone first tap may remain admitted for the full grace (default 6,000 ms); once a partner joins during the extension the cohort starts after a short 300 ms settle. Active sessions never mutate. The client receives the snapshot in `proximity:start` and applies the same scoring/timing. Admin tuning lives in **Settings**, separate from the live monitor. Test recordings store assignments, bounded diagnostics, derived outcomes, and the exact policy snapshot locally on the admin device.
**Consequences:** Operators can tune and refresh without redeploying, and every saved run remains interpretable. Weights must total 100; server bounds prevent unsafe windows. Policy changes affect only new sessions. The global cap remains 100 participants (up to 50 pairs), while the configured six-device cohort cap stays conservative even though the longer acoustic window has a higher theoretical slot ceiling. Admin run history is browser-local and is not a cross-operator database.

---

## ADR-0010 · 2026-07-02 · Admin readiness reflects real health/proof; dashboard is mobile-first and bilingual
**Status:** Accepted (in code)
**Context:** Operators run the admin dashboard from phones during physical test days and share it with several helpers. Three problems surfaced: (1) the dashboard showed a false "22% ready / server offline" whenever the browser had no admin token, because the token-gated `/api/diagnostics-public` snapshot failure cascaded into `serverReachable = false` even though the public `/readyz` was healthy — 22% is literally the server-unreachable branch (2 of 9 checks); (2) the "readiness %" ignored that 1-on-1 proximity pairing already works in live runs; (3) the desktop Settings form was a cramped 3-column grid and the Test cases tab overflowed horizontally on phones, and the test-case catalog was English-only. Auth is a single shared `METRICS_API_TOKEN` compared by exact string match on the server, so distinct per-user tokens are impossible without a server change + VM deploy — out of scope for the test day.
**Decision:** (A) Separate reachability from authorization in `refreshDiagnostics`: the public `/readyz` and proximity-policy reads decide `serverReachable`; a missing/invalid token only withholds the authenticated live snapshot and surfaces a non-alarming `adminTokenNeeded` banner, never a false offline. (B) Represent proven-but-partial capability with a new **"partial"** readiness status (proximity ceremony = "1-on-1 verified", ◐) that counts in `readinessSummary`, yielding a truthful 56% on a healthy server rather than a hardcoded figure. (C) Make the dashboard mobile-first: 2-column Settings fieldsets + full-width action row, ≥9.5px font floor for helper text, and `.test-main { grid-template-columns: minmax(0,1fr) }` + `.test-main > section { min-width:0 }` to stop grid children from forcing overflow. (D) Localize the 7-case test catalog into Japanese via a `TEST_CASE_I18N_JA` override map + `localizeCase()` while keeping `test-runs.js` (English) as the single source of truth. Keep the single shared token; the operator distributes it to helpers over a secure channel.
**Consequences:** Tokenless viewers (helpers who haven't pasted the token yet) now see accurate server health and clear guidance instead of a scary false-offline. The readiness number tracks real progress and will rise as multi-device/two-pair proof lands (flip the proximity item off "partial"). No backend change: per-user tokens/revocation would require adding a token list to `hasMetricsAuthorization` and redeploying the Azure VM. Cache correctness required bumping 1.0.93→1.0.94 across all `?v=` suffixes, both `APP_VERSION` constants, and the package manifests together (see `errors.md`).

---

## ADR-0011 · 2026-07-02 · Service worker must not touch the cross-origin API; admin console restructured for live testing
**Status:** Accepted (in code)
**Context:** From an emulated iPhone view the deployed admin page showed a false "Offline" with a `NetworkError`, while desktop/curl worked. Root cause: the service worker's generic `fetch` branch intercepted **cross-origin** requests too — every call to the Azure signaling server (`/readyz`, `/api/proximity-policy`, `/api/diagnostics-public`, and by extension the live channel) — and lacked error handling, so any transient failure (notably during the SW update that follows a version bump) surfaced as a failed fetch and, via `refreshDiagnostics`, flipped the whole page to "unreachable". CORS was verified correct (vercel + localhost origins get `Access-Control-Allow-Origin`, preflight 204). Separately, the console needed real-use ergonomics: the summary strip showed on every tab; the readiness board was a wide non-collapsible grid with an "eye"-looking status glyph; single-device signals were mixed with pairing concepts and a duplicate "Active proximity sessions" panel; timing tuning used bare number fields with no guidance; and points showed decimals.
**Decision:** (A) The service worker early-returns for any request whose origin ≠ `self.location.origin`, so cross-origin API/WS traffic bypasses the SW entirely (native fetch, like curl); the same-origin fallback branch also gained a cache `.catch`. (B) `isServerHealthy()` additionally trusts a live WebSocket (`isSocketLive()`) as proof of reachability so a transient HTTP hiccup can't show a false Offline. (C) The summary strip lives inside the Readiness panel only. (D) The readiness board is a vertical accordion (`state.readinessCollapsed`) with clearer icons. (E) Live testing is a vertical stack: Connected devices carries an inline pairing badge (`deviceSessionState`, replacing the separate sessions panel), a Single-device test, a new Two-device test (`renderMultiDevice` — two devices side-by-side + live pairing outcome from session data, reusing the existing monitor telemetry), and an event timeline showing the latest 4 with a show-more toggle. (F) Settings timing windows are slider + number box with plain-language explanations and recommended/allowed range notes; weight total and minimum score are integer and aligned. No server/policy change (min score stays 55, revision 2).
**Consequences:** Cross-origin diagnostics are now immune to SW-induced failures, which was the real fix for the false Offline; the WS-reachable fallback is defense-in-depth. The two-device panel is pragmatic — it reuses per-device monitor telemetry plus whatever the proximity session exposes, so full reciprocal detail only appears once a real ceremony runs between the chosen pair. Some old session-render helpers are now dead code (left in place; safe to prune later). Cache-affecting → bumped 1.0.94→1.0.95 across all `?v=`, both `APP_VERSION`, and manifests.

---

## ADR-0012 · 2026-07-02 · Single-device test drops reciprocal-audio rows; two-device test shows full per-device panels
**Status:** Accepted (in code)
**Context:** Operator feedback on the v1.0.95 console: (1) the single-device test still listed "Heard signal / Correlation / Energy margin", but a lone phone in the monitor emits *and* listens, so those rows only reflect it hearing its own chirp — misleading self-noise, not a proximity result; (2) the two-device test's compact 4-row columns were too thin — the operator wants each phone's full live frequency map and complete signal readout side-by-side, with the pairing outcome; (3) the connection state was shown twice (top-bar "Connected" label + summary strip/inline status); (4) Settings looked cramped: timing windows were a 3-column grid with tiny text, the weight total had a stray `border-left`, and the minimum-score control wrapped to its own line; (5) test cases had no visual for how to physically hold the phones; (6) the "Live updates" toggle and a "refreshes every second" footer line were noise; (7) the session-table render helpers left dead by ADR-0011 were still present.
**Decision:** (A) Refactor the monitor renderers into pure builders — `frequencyChannelsHtml()`/`frequencySpectrumBlockHtml()` and `metricDefinitions()/metricTableHtml(telemetry, mode)` — and tag reciprocal rows `pairOnly`. The single-device table renders `mode:"single"` (Emitted packet, Sample rate, Bump, Tilt only); each two-device column renders `mode:"pair"` as a full panel (frequency map + all rows, compacted to metric·current·status via CSS). `latestSessionEvidence/latestEventEvidence` gained an optional deviceId so per-column evidence resolves correctly. (B) Removed the top-bar connection label; the single live `[data-server-connection]` chip now lives in the Live devices heading and is driven by `renderSummary` (guarded for absence). (C) Polling is always on — deleted the `data-live-poll` toggle. (D) Settings: `.score-summary` row aligns total + minimum on one line, `border-left` removed; timing windows are single-column cards; policy-revision display removed; primary button relabeled "Save & apply" (unchanged behavior — `updateProximityPolicy` applies to the signaling server instantly, no Vercel redeploy). (E) Added inline SVG hold-diagrams (`phoneSvg`/`testCaseIllustration`) per case. (F) Purged the dead session-table cluster + orphaned i18n keys. No server/policy change (min score 55, revision 2).
**Consequences:** The single-device test now only asserts what one phone can truthfully prove; the two-device test is the sole place reciprocal audio is judged. The two-device columns still depend on live per-device monitor telemetry + the proximity session, so full data appears only once both phones are monitored and a ceremony runs. Score-weight changes still apply server-side on Save with no code deploy; a *code* change (like this one) is what needs the Vercel push + version bump. Cache-affecting → bumped 1.0.95→1.0.96 across all `?v=`, both `APP_VERSION`, and manifests.

---

## ADR-0013 · 2026-07-02 · Roster prunes departed peers; View reserves a tab every click; SW hard-resets caches per version
**Status:** Accepted (in code)
**Context:** Operator-reported bugs on the live app: (1) ghost peer avatars stayed on the orbit after a nearby device left — the server's targeted `peer:disconnected` only tore down the *connected* session, never removing a merely-nearby peer from `state.peers`, and offline roster entries (`online:false`) were rendered as connectable; (2) tapping "View" on a received image opened a new tab the first time but navigated the *current* tab on later taps, because a blank tab was reserved only when the blob URL didn't yet exist (first click); (3) the operator wanted a guaranteed clean cache on each release; (4) the microphone re-prompts on every page refresh even after being granted.
**Decision:** (A) `peerDisconnected` now always filters the departed `peerId` out of `state.peers` before the session-teardown logic, and `rankPeersForDisplay` drops any peer with `online === false`, so ghosts and offline devices never appear on the orbit. (B) The `open-received` handler reserves a fresh tab synchronously (`reserveBrowserView`) for *every* view intent on a previewable item — including the cached-URL fast path — and passes it to `openReceivedUrl`, so View reliably opens a new page each time (mobile-safe within the user gesture). (C) The service worker `activate` deletes *every* Cache Storage entry then rebuilds the current precache from the network (`install` only `skipWaiting()`s); with the version bump this fully clears each user's cache on update. (D) The mic re-prompt is an iOS Safari platform limitation (getUserMedia grants are not persisted per page load in a Safari tab; only a home-screen PWA persists them) — the code is already lazy, gesture-gated, and Permissions-API-aware, so no code change was made; the fix is to install the site to the Home Screen. (E) Admin hold-diagrams corrected to match reality: cross-angle is a true X (~30° each, procedure text updated en+ja), negative-control shows a connecting Pair A + isolated non-bumping Pair B with a no-connect badge, noisy-room adds ambient sound-wave arcs and background phones.
**Consequences:** Orbit presence now tracks real device comings/goings client-side even if the server only sends targeted disconnects. View is consistent across taps and platforms. Every release is a clean-cache release (slightly higher first-load cost after an update, which is the intended trade for zero stale assets). iOS mic re-prompt remains until the operator installs the PWA — documented, not silently "fixed". Cache-affecting → bumped 1.0.96→1.0.97 across all `?v=`, both `APP_VERSION`, and manifests.

---

## ADR-0014 · 2026-07-02 · Lower bump-detection threshold to 4.5 m/s² for real-world reliability
**Status:** Accepted (in code)
**Context:** In live large-scale physical testing the admin dashboard's live status kept reporting bump "not detected", and the server rejected otherwise-valid sessions with `bump_not_detected`. The bump gate requires the client's `motion.bump` boolean (ultrasound + bump + tilt must all be present). `motion.bump` flips true when per-sample `linearAcceleration >= BUMP_ACCELERATION_THRESHOLD (6)` or `gravityDelta >= 2.5`. Real phone-to-phone bumps frequently peak below 6 m/s² as measured: a phone case softens the impact, and `devicemotion` samples at only ~60 Hz so a sharp, short impulse often lands between samples and the captured peak understates the true jerk. In the continuous admin monitor `motion.bump` is sticky (never reset mid-session), so a persistent "not detected" means the threshold was simply never crossed — i.e. it was set too high, not a capture or timing bug (the ceremony's motion window already spans permissions → sync → the full acoustic exchange).
**Decision:** Lower `BUMP_ACCELERATION_THRESHOLD` 6→4.5 and `gravityBumpThreshold` 2.5→2.0 (both still clearly above the <3 passive-handling noise floor). Align the admin bump-tone heuristic in `readiness.js` from `>=6` to `>=4.5`. No server change — the client's `bump` boolean is authoritative for the server's evidence gate. This continues the established trajectory (10→6 previously, now 6→4.5). Operators can read the live raw peak in the admin Single-device test "Bump evidence" row ("raw X") and we can tune further from real numbers if needed.
**Consequences:** More real bumps register, reducing false `bump_not_detected` failures, at a small risk of a firm pickup registering as a bump — acceptable for the pairing model, which also requires reciprocal ultrasound + tilt. Tests use the exported constant dynamically so they stay green. Cache-affecting → bumped 1.0.97→1.0.98.

---

## ADR-0015 · 2026-07-02 · Failure message reflects reciprocal-acoustic reality, not a phantom "no bump"
**Status:** Accepted (in code)
**Context:** In live testing a phone showed score 92/100 with audio, bump (+20) and tilt (50°) all green, yet the ceremony reported "No matching bump found. Try again or use QR." The audio card read "Energy heard 1/2" — i.e. `acoustic.energyAssisted = true`: the phone detected ultrasonic energy in-band but never decoded the partner's specific coded chirp. The server's anti-relay reciprocity gate (`hasReciprocalAcousticEvidence`, signaling-hub.js) requires each phone's `heardAcousticSignatureId` to equal the partner's signature and `acousticConfidenceMargin ≥ ACOUSTIC_WINNER_MARGIN (0.04)`; an energy-only detection satisfies neither, so the pair is skipped in `tryMatchProximitySession` and the session times out. On the client, `proximityFailureMessages` returned no local errors (all of this device's own evidence was present), so `failAnonymousVerification` fell through to the generic `proximityMatchFailed` string ("No matching bump found") — actively misleading, since bump was +20 and the real problem was acoustic reciprocity.
**Decision:** When `failAnonymousVerification` has no concrete local errors, show `proximityErrorReciprocal` ("The phones didn't hear each other's chime. Hold them closer with screens up, then try again.") instead of the generic match-failed text, and only defer to a mapped server reason when it is specific/actionable (not the generic fallback). Do **not** weaken the server's reciprocity gate — it is the ADR-0003 anti-relay core, and the Azure signaling server is a separate VM deploy this repo's Vercel push does not touch. The underlying pairing failure is acoustic/operational, not a client bug: the live policy `acousticWindowMs` was 6000 ms, too short for reliable coded-chirp decode, so phones fall back to energy-assisted.
**Consequences:** Operators stop chasing a non-existent bump problem. Reliability levers are runtime-tunable via admin Settings → Save (pushed to the Azure server, no redeploy): raise Acoustic exchange window (~9000 ms) for more chirp repeats → clean decode → real reciprocal match; raise Bump match slop for looser bump-time pairing; plus operational (closer, screens up, quieter room, fewer concurrent pairs) or the QR fallback. If clean decode still can't be achieved at scale, a future decision could let strong energy-assisted + bump-time correlation match — but that is a deliberate security trade for the maintainers, not a silent change. Cache-affecting → bumped 1.0.98→1.0.99.

---

## ADR-0016 · 2026-07-02 · Ack final proximity telemetry before waiting for server match
**Status:** Accepted (in code)
**Context:** After operators increased late-tap grace, acoustic exchange time, and bump slop, a real phone still failed with a high local score (92/100) and green Audio/Bump/Tilt cards. Live diagnostics showed the subtler failure: both devices could locally finish the ceremony, but the server's final matching decision only had accepted `proximity:session:telemetry` from one side before returning `ambiguous_or_nonreciprocal_match`. The existing server logged only client join/leave, so there was no concrete proof whether the missing side never sent telemetry, sent while its socket was closing, used a stale nonce, or was outside the server timing window. Increasing the gap alone cannot fix a final telemetry delivery/acceptance problem.
**Decision:** Add a server-acknowledged final telemetry handshake without weakening proximity criteria. The browser now waits for `proximity:session:telemetry:accepted` after sending `proximity:session:telemetry`, retries up to three short attempts while the session is still current, and emits bounded `proximity:session:diagnostic` events for send/accepted/unconfirmed states. The Azure hub now sends `proximity:session:telemetry:accepted` after storing accepted telemetry, and funnels all telemetry rejection paths through one structured logger/metrics path (`proximity:session:telemetry:rejected`) with session presence, nonce state, expiry state, and timing deltas. Existing `proximity:session:failed` remains the compatibility failure event.
**Consequences:** A transient lost/closed final telemetry frame gets a second chance before the controller waits for `proximity:match`; if the server rejects telemetry, the VM journal and diagnostics endpoint now expose the actual reason. The anti-relay security gate is unchanged: a match still needs reciprocal coded acoustic evidence plus bump and tilt. Cache-affecting frontend changes and protocol additions required bumping 1.0.99→1.0.100 and deploying both the Vercel/static frontend and the Azure VM backend.

---

## ADR-0017 · 2026-07-02 · Safari microphone denial cache must not suppress the next user gesture
**Status:** Accepted (in code)
**Context:** Safari users reported that microphone access was not showing. The relevant controller path cached `webdrop.proximityPermissions.microphone = "denied"` in `localStorage`; once present, `ensureProximityPermissions()` returned a cached denial instead of calling `getUserMedia()` on future Connect/admin-monitor taps. That is safe-ish for browsers with a trustworthy Permissions API denial, but it is wrong for Safari/iOS: microphone prompts are gesture/page-state sensitive, Safari may not expose `navigator.permissions.query({name:"microphone"})`, and a stale local value can outlive a changed browser/site setting.
**Decision:** Never use the app-local microphone cache to skip `getUserMedia()`. The controller now always calls `proximity.requestMicrophonePermission()` from the fresh user gesture. The microphone cache only persists `"granted"` or `"unsupported"`; denial/error results become `"unknown"`, and legacy stored `"denied"` is normalized to `"unknown"` on read. The acoustic sensor remains responsible for honoring a real browser-level Permissions API `denied` state where supported.
**Consequences:** Safari gets a new chance to show the native microphone prompt after a failed/aborted/changed permission attempt. Browsers with a genuine Permissions API denial still avoid a doomed prompt inside `AcousticProximitySensor`. Added `tests/controller-permissions.test.mjs` for the migration and non-persistence rule. Cache-affecting → bumped 1.0.100→1.0.101.

---

## ADR-0018 · 2026-07-02 · Admin token prompt must fail closed without console errors
**Status:** Accepted (in code)
**Context:** Browser-smoke verification of production `/admin/?tab=settings` after v1.0.101 showed the Settings tab and tuning controls rendered, but console logs contained `prompt() is not supported` from `promptForAdminToken()`. The diagnostics endpoint is token-gated; tokenless helpers are expected to see public readiness + a guidance banner. A browser surface that disables modal prompts should not make the dashboard look broken or noisy.
**Decision:** Wrap `globalThis.prompt()` in `promptForAdminToken()` and treat missing/throwing prompts as "no token entered" by setting `tokenPromptDismissed` and returning an empty string. Keep the existing behavior for normal browsers where prompt works, including saving a pasted token to `sessionStorage`.
**Consequences:** Production admin pages render cleanly in embedded/no-prompt browser surfaces. Tokenless users still do not receive authenticated diagnostics; they must paste/configure the shared operations token in a supported browser path. Cache-affecting → bumped 1.0.101→1.0.102.

---

## ADR-0019 · 2026-07-02 · Admin authentication must be a visible session-only workflow
**Status:** Accepted (supersedes ADR-0018)
**Context:** Production v1.0.102 avoided the embedded-browser `prompt()` exception, but that exposed a worse usability failure: the page told operators to paste a token while rendering no input or action. Live devices and diagnostics remained empty, so the admin dashboard was technically rendered but operationally unusable. Production also attempted the gitignored local-token URL and made a guaranteed unauthorized private diagnostics request before it knew whether a token existed.
**Decision:** Replace modal token collection with a responsive bilingual **Admin access** form in the page. Store a submitted token only in per-tab `sessionStorage`, validate it through the real diagnostics request, show an inline invalid-token error, and hide the form after authorization. Auto-load `local-admin-token.js` only on localhost. When no token exists, read public readiness/proximity policy but skip the private diagnostics request entirely. Keep Settings readable but prevent Save until authorization is established.
**Consequences:** Operators always have an explicit way to authenticate, including Safari and embedded browsers that disable modal prompts. Tokenless production loads without 404/401 console noise, while the bearer token remains out of committed assets and persistent storage. Existing shared-token server auth is unchanged; this is a frontend-only cache-affecting release, bumped 1.0.102→1.0.103.

---

## ADR-0020 · 2026-07-03 · Reliability overhaul — deterministic pairing, iOS mic hardening, band expansion, multi-tab, magic link
**Status:** Accepted (in code; frontend on Vercel 1.0.104 + Azure VM signaling-hub.js both deployed & verified 2026-07-03)
**Context:** Multi-hour, multi-person field testing exposed systemic pairing failure: ~70-80% success and only with a folk trick ("tap Connect, wait 3-5s, then bump"); immediate bumps almost always failed; some phones' microphones went "dead" until switching browsers; a second open tab broke the first. Live diagnostics (`/api/diagnostics-public`) + a full code trace (three parallel Explore agents + a Plan agent) pinned exact root causes: (RC1) server `ceremonyTimingValid` floor `bumpAt >= startAt-250` while `startAt = bothJoined + startDelay(1200) + stagger` — so the valid bump window opened 1.2-2.4s after the second tap and an immediate bump was rejected `timing_out_of_window` before analysis. (RC2) iOS silent capture: `emitted=true, detected=false, rms≈0.001` for the whole window despite the phone emitting its own chirp — caused by reusing an iOS-muted-but-`active` stream, an AudioContext created before getUserMedia at a mismatched sample rate (44.1 vs 48k → WebKit `createMediaStreamSource` captures silence), no `interrupted` handling, a never-rebuilt stale capture graph, and a priming pulse (gain 0.00001) below the iOS session-activation threshold. (RC3) chirp band 18.6-19.4kHz is weak on phone speakers and splits into only 1 lane, so concurrent pairs interfere. (RC4) decode ran only at end-of-window → ~11-13s always. (RC5) motion permission persisted `"denied"` and short-circuited the gesture prompt forever. (RC6) the WS client reconnected on a 4001 takeover close, fighting the newer tab. (RC7) admin required pasting a token per browser.
**Decision:** Ship as v1.0.104 in 7 fixes. (1) Server floor → `min(createdAt-2000, startAt-250)` (production accepts a tap-time bump; test fixtures with `startAt<<createdAt` keep the old floor). Client island cues "Hold together"→"Bump now" (en/ja). Anti-relay unchanged (reciprocal acoustic + pair bump-delta). (2) `acoustic-proximity.js` audio-lifecycle rebuild: `#streamHealthy()` (drops muted/ended tracks), track mute/ended listeners, context sample-rate pinned to the track rate, `statechange`/`interrupted` resume, rebuild-aware graph keyed on stream+context identity, 18.5kHz/0.15 priming pulse, `captureSelfTest()` run in the ceremony sync headroom to rebuild a dead mic before the window, and `revalidateAudio()` on foreground. (3) Band → 17.8-20.0kHz (4×550Hz lanes); client `MIN_INAUDIBLE_FREQUENCY_HZ`→17500. **User explicitly accepted faint audibility to young ears** for reliability + concurrency. (4) Client early-decode (`peekCeremonyCapture` + mid-window decode, finishes on a clean partner decode; end-of-window decode is the unchanged fallback) + server `startDelay`700/`stagger`300. (5) Motion: only `"unsupported"` short-circuits; `restorePermission("denied")→"unknown"`. (6) WS 4001/`replaced_by*` close is terminal (no reconnect), emits `replaced`; app shows an overlay + "Use here" (`connect({force:true})`); `BroadcastChannel` standdown. (7) Admin `/admin/#k=TOKEN` fragment magic link → sessionStorage + URL scrub; paste form stays fallback; policy writes stay Bearer-gated (no server change for #7).
**Consequences:** Immediate-bump pairing works with no user trick; a silently-dead iOS mic is detected and rebuilt before it can fail the ceremony; concurrent pairs get separate lanes; connect drops to ~5-7s. **Rollout is order-sensitive:** deploy the frontend (Vercel) FIRST and confirm the new SW version is live, THEN deploy the Azure VM — an old cached client refuses to emit below 18.5kHz (`supportsInaudibleChirp`→`emitted=false`), so the server band must not lead the client. The server changes (band defaults, `ceremonyTimingValid` floor, `startDelay`/`stagger`) live in `azure cloud server/src/signaling-hub.js` and require a manual VM deploy this repo's push does not perform. After the server deploy, set the admin policy acoustic window to ≈5000-6000ms. The reciprocal-acoustic anti-relay gate, decode thresholds, score weights, and min-score 55 are deliberately unchanged. Tests: new early-bump acceptance (server, 55 total), new 4001 WS test (frontend, 3 total in that file), updated band/getStatus assertions; `verify:full` green (frontend 53, server 55, 0 vulns).

---

## ADR-0021 · 2026-07-03 · Cap the acoustic band at 19.8kHz for 44.1kHz-device Nyquist safety (post-v1.0.104 audit, v1.0.105)
**Status:** Accepted (in code; frontend Vercel 1.0.105 + Azure VM signaling-hub.js — see state.md for deploy status)
**Context:** A complete post-v1.0.104 audit (3 parallel Explore audits + Plan + first-hand re-verification of every finding), focused on **concurrent devices**, found the 17.8-20.0kHz band from ADR-0020 broken on 44.1kHz-sample-rate devices (older iPhones, many desktops, some Androids). The client caps emittable frequency at `sampleRate * 0.45` (`supportsInaudibleChirp`) → **19,845 Hz on 44.1kHz hardware**, and `createChirpSamples` returns a *silent* buffer (and `emitChirp` refuses) when a lane exceeds that cap. The server split 17,800-20,000 into 4 lanes of 550 Hz, so **lane 3 (19,450-20,000 Hz) > 19,845** — a 44.1kHz phone round-robin-assigned that lane emitted silence and built silent decode templates. With concurrent sessions lane assignment cycles, so **every 4th cohort failed acoustics** if either phone ran at 44.1kHz (`acoustic_not_detected`, "works on retry"). The same audit found 8 other bugs (2 critical, see ADR context and state.md).
**Decision:** Lower `ACOUSTIC_BAND_END_HZ` **20,000 → 19,800** (server `signaling-hub.js`). Lanes become 4 × 500 Hz (≥ the 420 Hz minimum), top lane **19,300-19,800 ≤ 19,845** → every lane emits on 44.1kHz **and** 48kHz hardware. Mirror the cap everywhere the old 20,000 leaked: server `message-schema.js` admin-monitor clamp `[17_000, 21_000]` defaults `17,800/19,800` (was `18,500/…` — it silently clamped admin monitors above the new low lanes, blinding operators to lane 0-1); client monitor defaults + admin analysis band splits (`controller.js`, `admin/readiness.js`) 20,000/20,500 → 19,800. Preferred long-term hardening (not shipped): server could pick each cohort's band end as `min(0.45·min(cohortSampleRates), bandEnd)` from the `acousticCapabilities.sampleRate` clients already send on join — unnecessary once 19,800 is the fixed ceiling.
**Consequences:** All 4 concurrent lanes work on both common sample rates, closing the 1-in-4 concurrent-cohort failure. The band shrinks 200 Hz (2,200→2,000 Hz total); still 4 lanes ≥ 420 Hz, no capacity loss. **Same order-sensitive rollout as ADR-0020** (frontend first, then VM) — though here direction is safe both ways since 19,800 < both the old client floor and the emit cap. Shipped in v1.0.105 alongside 8 other audit fixes (reclaim-tab payload reuse, capture-lead decode alignment, self-test cleanup + re-acquire timeout, 2-phone early-start linger, tolerant SW precache, idempotent duplicate-manifest ack, peer filename sanitization); zero-byte transfers were verified already-correct (no change). `verify:full` green (frontend 57, server 55, 0 vulns).

---

## ADR-0022 · 2026-07-04 · First-attempt pairing fixes (lane-0/1 filter fossil, telemetry expiry race, tap-time acoustic preflight) + staged ceremony UI (v1.0.106)
**Status:** Accepted (in code; frontend Vercel 1.0.106 + Azure VM signaling-hub.js — see state.md for deploy status)
**Context:** Field-testing v1.0.105 (two phones) showed the FIRST 2-3 pairing attempts on a fresh page load failing with "the phones didn't hear each other's chime", then succeeding on retry. Diagnosis (2 Explore audits + Plan agent + first-hand VM-journal/diagnostics reads) found THREE compounding defects. **(P0, the deterministic one)** `js/services/proximity-engine.js` `normalizeAcousticPlan` filtered signature plan entries to `startFrequencyHz >= 18_500` — a fossil from the pre-v1.0.104 band floor (introduced when the band started at 18.5kHz). After ADR-0020/0021 moved the band to 17.8-19.8kHz split into 4 lanes (17800-18300, 18300-18800, 18800-19300, 19300-19800), **lanes 0 and 1 start below 18,500 and were dropped by every client** → `signatures = []` → neither phone emitted, decode ran an empty plan → guaranteed reciprocal acoustic failure regardless of mic health. The server assigns `bandIndex = proximityBandCounter++` (`% 4`), a server-lifetime counter, so consecutive sessions cycle lanes 0,1,2,3 — and the VM had just restarted (counter=0), so the user's first two attempts landed on the two dead lanes and the third worked. Today's live metrics: 28 started / 10 matched / 17 failed ≈ the dead-lane fraction. **(P1, live-confirmed)** `signaling-hub.js` set `session.expiresAt = creation + proximitySessionTtlMs(15000)` only at creation and never extended it at start; the ceremony spends ~10.5-11.75s of that window, and the `failTimer` deliberately lingers `matchSlop(5000)` past window end for late telemetry — but the telemetry gate rejected `session.expiresAt <= Date.now()` as `session_not_available`. Live 04:34 log: iPhone final telemetry at endsAt+2.2s rejected 3× with `timing.valid:true` → one-sided telemetry → `ambiguous_or_nonreciprocal_match`. Late-tap-grace sessions (join extension +8s) could have endsAt > creation+15s → ALL their telemetry rejected. **(P2, iOS warm-up)** `prepareAudioOutput()` created the AudioContext during the permission phase before the mic stream existed → default sample rate; `#reconcileContextSampleRate` only ran inside `startCeremonyCapture`, so the first capture could run a rate-mismatched graph (WebKit resample kills 17.8-19.8kHz → silent capture); and the v1.0.104/5 sync-phase self-test safety net was starved because `hasHeadroom()` needs `startAt-now >= 900` while the server sends `startAt = now + 700 + stagger` (stagger 0 for the first/only cohort) — so at stagger 0 the whole self-test was skipped. Separately, the user asked to replace the permissions/audio/bump/tilt checklist with one clean staged animation.
**Decision:** **(P0)** compare against `MIN_INAUDIBLE_FREQUENCY_HZ` (17,500) instead of the hardcoded 18,500. **(P1)** in `startProximitySession`, after `session.endsAt = startAt + durationMs`, extend `session.expiresAt = Math.max(session.expiresAt, session.endsAt + matchSlopMs + 2000)` so telemetry stays acceptable for the whole failTimer linger (audited every other proximity `expiresAt` reader — all gated on `!started`/pre-start; no TTL sweep, no leak). **(P2)** reconcile the context to the live track rate the moment `ensureProximityPermissions`' `Promise.all` settles (new `reconcileContextToTrackRate()` — NOT inside `requestMicrophonePermission`, which would race `prepareAudioOutput`'s concurrent resume and flip `audioOutput.granted=false`, disabling acoustics); and add a single-flight `preflightCapture()` fired at the Connect tap that warms up / rebuilds the emit→mic→capture loop concurrently with the join window (bounded ~2.5s, narrow 18.5kHz probe that can't spoof a coded-sweep decode), bounded-awaited in the sync phase as a freshness handshake that skips the now-usually-starved self-test. **(P3 UI)** replaced the 3-row checklist with a monotonic 6-stage ladder in `DynamicIsland` (Getting ready → Exchanging ultrasonic waves… → Bump now → Bump detected/verifying tilt → Verifying proximity… → Connecting…) driven by the same ceremony progress events, rendered as one status line + a 6-segment step-dot row over the tile-wave; the bump cue stays prominent; failure keeps the score/error/Retry/QR contract; deleted the metric checklist DOM/CSS/i18n and the acoustic-status formatters. `renderVerifiedCeremony` was reimplemented (not deleted — it is called after `proximity:match`).
**Consequences:** First-attempt pairing works on all four lanes (lane 0 is literally the first session after a VM restart, so this is directly testable); late-but-valid telemetry is accepted; a first-attempt dead/rate-mismatched capture is rebuilt before it can fail the ceremony; the ceremony UI is a single calm staged flow instead of a jittery checklist. P1 is a server change (one VM redeploy) and is backward-compatible with old clients; P0/P2/P3 are client-only. Same frontend-first rollout as ADR-0020. Tests: lane-0/1 ceremony regression + 4 preflight unit tests (`tests/proximity-engine.test.mjs`), server expiry A/B (`signaling-hub-proximity-session.test.mjs`), rewritten staged-ladder e2e (`app-ui.spec.mjs`). `verify:full` green (frontend 62, server 57, 0 vulns). Bumped 1.0.105→1.0.106.

## ADR-0023 · 2026-07-09 · Multi-pair cohort matcher: match all disjoint pairs + bump-partner veto (server-only)

**Context.** 4-device / 2-pair field test: one pair connected while the other never did, and on retry a WRONG (cross) pair connected even with the pairs meters apart. Two defects in `tryMatchProximitySession`: (1) it matched ONE best-delta pair per telemetry event and `return`ed when that best pair was stale (member disconnected or already invite/QR-paired) — the stale pair stayed "best" on every later call and permanently starved the other valid pair; (2) pairing identity rested solely on mutual loudest-decode reciprocity — when each true pair has one weak/blocked emitter (the classic first-attempt failure), the two working phones across pairs hear each other as top decode, pass the reciprocity gate, and get connected whenever their independent bumps land inside matchSlop (4–5s; 18–19kHz carries many meters indoors, so separation does not protect).

**Decision.** (a) Collect ALL eligible pairs (delta ≤ slop + reciprocal evidence), sort by bump delta, and greedily activate every disjoint pair, skipping (not aborting on) stale/conflicted candidates. (b) New `hasCloserBumpPartner` veto with `BUMP_PARTNER_VETO_MARGIN_MS = 700`: a genuine bump is one physical event, so partners' bumpAt land within jitter of each other; if any other unmatched cohort participant's accepted bump is ≥700ms closer to either member than the pair's own delta, the pair is vetoed. Honest failure (retry) beats connecting strangers. The ultrasound+bump+tilt evidence gate is untouched — this only TIGHTENS matching.

**Consequences.** Both symptoms covered by new regression tests that fail on the old matcher. Residual wrong-pair window narrows from matchSlop (4-5s) to <700ms bump coincidence combined with double emitter failure. A true pair with an unusually large internal bump delta (>700ms) plus a third device bumping coincidentally close to one partner can now be vetoed into an honest retry — accepted trade-off, tunable via the margin constant.

## ADR-0024 · 2026-07-09 · Discard pre-window motion at ceremony startAt (phantom "bump detected" on 2nd device / reconnect; client-only)

**Context.** Field report: the second device of a pair — and any device retrying or reconnecting — showed "Bump detected" without a physical bump, so pairs could connect on phantom evidence. Root cause chain: the controller starts motion capture at permission-resolve time, well before the server's `startAt` (it must — the prompts have to run inside the user gesture and the sensor needs warm-up); `MotionProximitySensor.getSnapshot()` LATCHES evidence (`bump: this.snapshot.bump || bump`, `bumpAt` set once, tilt likewise) and only `reset()` clears it; the controller's `resetMotionCapture()` runs *before* capture starts. So every jolt between the Connect tap and `startAt` — the tap itself, picking the phone up, repositioning — latched into the snapshot and survived to the ceremony's evidence read. Masked on a fresh first connect because the motion-permission prompt forces a pause between tap and window (and the user then genuinely bumps); on the 2nd device and on reconnects permissions are cached, so the Connect-tap jolt lands immediately before the window and stamps a phantom `bump=true`.

**Decision.** Reset the latch exactly at the window open, inside the engine: `runRealCeremony` calls `this.motion.reset?.()` immediately after `waitUntil(startAt)`, before the acoustic phase. Bump AND tilt must now occur DURING the guided window — which is what the staged UI instructs ("Bump now"). The listener stays attached, so samples re-accumulate immediately (~60Hz); the zero-samples `canFinishEarly` bypass is therefore only milliseconds wide and the early exit additionally requires a clean partner decode (≥ one chirp slot), so there is no practical interaction. The `?.` is on the method only — it keeps existing engine mocks (fixed `getSnapshot()`, no `reset`) valid, while `this.motion` itself is always set (constructor defaults to `new MotionProximitySensor()`; the ceremony already dereferences it unconditionally). Deliberately NOT fixed controller-side: the controller cannot hit `startAt` as precisely, and capture must still start early — the latch is the problem, not the early start.

**Consequences.** Phantom bump on 2nd device / reconnect eliminated. Both partners reset at the same server `startAt`, so the discard is symmetric and server-side bump-delta matching (matchSlop, the ADR-0023 veto) is unaffected. Trade-off: a real bump made BEFORE the "Bump now" stage no longer counts — users must bump during the window, as the UI already directs. Client-only; no server change. Regression test "a bump latched before the window opens is discarded at startAt" uses a mock whose `reset()` flips a pre-window latched bump to the in-window state — the phantom case must fail locally, the real in-window case must pass. `npm run verify` green (**64/64**).

## ADR-0025 · 2026-07-09 · Multi-pair cohorts: 2-device-only winner margin + cross-cohort bump veto with deferred matching (server-only)

**Context.** Live 4-phone / 2-pair field test on v1.0.110, watched over the diagnostics feed: pair-at-a-time matched at 0.92; both pairs together failed two distinct ways. **(a) One 4-device cohort:** every phone verified 0.92 with acoustics detected, and the only reciprocal top-decode pair — a TRUE pair — was killed by `hasSufficientWinnerMargin`. All cohort members share ONE band, so the runner-up decode is a real phone ~2m away and healthy margins collapse (field: partner 0.352 vs other pair's phone 0.324 = 0.028 < `ACOUSTIC_WINNER_MARGIN` 0.04) → eligible list empty → the whole cohort timed out `ambiguous_or_nonreciprocal_match`. Loudness is also a poor partner signal in-room: one phone top-decoded the loudest speaker across pairs, not its partner. **(b) Staggered taps split the 4 phones into two 2-device cohorts by ARRIVAL ORDER — crossed against the physical pairs.** The crossed pair inside one cohort was formally reciprocal (18-19kHz carries meters indoors) with a 1681ms bump delta inside matchSlop and **connected the wrong pair**; each phone's true partner, whose bump landed 68-71ms away, was only visible in the OTHER cohort's telemetry — invisible to the per-session ADR-0023 veto. The wrong-pair cohort also matched before the other cohort's telemetry had even arrived.

**Decision.** (1) The winner-margin gate applies to **2-device cohorts only** (`proximityCohortSize(session) <= 2`, sized via max of signatures/clients/telemetry so mid-ceremony disconnects don't reclassify a 4-phone recording). In bigger cohorts partner identity is carried by top-decode reciprocity + the bump-partner veto. (2) `hasCloserBumpPartner` now also vetoes against **other started cohorts' bumps** (`foreignProximityBumpTimes`, matched entries excluded, epoch-ms bumpAt is cross-session comparable), and `tryMatchProximitySession` **defers** activating an eligible pair while a concurrent overlapping cohort (started, unexpired, `endsAt + slop` in the future) still owes telemetry. Re-evaluation is event-driven — every accepted telemetry runs `tryMatchStartedProximitySessions()`, and foreign-session teardown (failUnmatched, cancel) re-runs it — plus `failUnmatchedProximitySession` **force-evaluates** the session at its own fail deadline, so deferral can delay a match but never starve one into a failure.

**Consequences.** A 4-device cohort matches its reciprocal true pair (the other, crossed-decode pair fails honestly and succeeds on retry); crossed split cohorts are vetoed into honest retries instead of connecting strangers. A tight true pair (delta < 700ms) can mathematically never be cross-vetoed (the foreign bump would need to be ≥700ms closer than a sub-700ms delta). Deferral adds latency only when concurrent cohorts overlap — single-pair sessions are untouched. Residual by design: two pairs bumping on the same beat (<~700ms apart) remain informationally ambiguous → honest retry; field protocol is to bump pairs a few seconds apart. Tests: 4 new server cases (collapsed-margin cohort, crossed split cohorts, defer-then-match, force-match-at-deadline), all verified to FAIL on the pre-fix matcher; suite 64/64, `node --check` + `npm audit` clean. Deployed: commit `7710bc8`, VM file sha256 `7b8b29a2…` verified identical to local.

## ADR-0026 · 2026-07-09 · Abort doomed ceremonies on denied motion/mic (client, v1.0.111) + hold cohort matches for unreported members (server)

**Context.** Post-ADR-0025 monitored field test: the matcher fixes worked (11 clean matches, zero wrong pairs), but two residual defects surfaced. **(a)** After a ~35-min break, one phone's page reload led to Motion & Orientation being denied (07:07, `permissions:complete failed reason=motion:denied`); iOS then auto-rejects every further permission request for the rest of the page session without showing a prompt, and the sensor caches the denial too — yet the app ran the full staged bump ceremony anyway, four times, each doomed (samples 0, score pinned at 0.34 = acoustic only; server correctly rejected) with no hint of the real problem, and each attempt dragged the partner phone into a guaranteed-fail cohort. **(b)** A 3-device cohort (06:30) matched a sloppy 1406ms-bump-delta pair the moment the second telemetry landed — two seconds before the third member reported a bump only 452ms from one of them, which the ADR-0023 veto would have rejected; the ADR-0025 deferral only waited on OTHER cohorts' pending telemetry, not the same cohort's.

**Decision.** **(Client)** Module-level `blockedProximityPermissionKey` (unit-testable, exported via `__controllerTest`) classifies denied/errored motion or microphone permissions ("unsupported" deliberately not blocked — those devices keep the QR-fallback flows). The anonymous path aborts BEFORE `joinProximitySession` via `failAnonymousVerification` with one actionable message ("Motion access is off… reload the page and allow it", en/ja); both real-ceremony functions early-return `reason: "permission-denied"` as belt-and-braces; `proximityFailureMessages` maps `denied` to the actionable strings and, for permission-denied results, suppresses the consequential bump/tilt/score noise. **(Server)** `shouldDeferProximityMatch` also defers while `session.telemetry.size < session.clients.size` and the session's own window+slop is open — free for 2-device cohorts (a pair needs both telemetries to be eligible anyway), and the existing telemetry-driven re-eval + force-eval at the fail deadline bound the wait.

**Consequences.** A phone with revoked motion/mic gets an instant, actionable failure instead of burning 15s per doomed ceremony, and its partner is no longer pulled into unwinnable sessions; retries with the denial still cached fail instantly with the same guidance (only a page reload recovers — by design of iOS). Multi-device cohorts now always see every member's bump before activating a pair, closing the last window where a closer-bump veto could arrive too late. Tests: controller key-classification unit test; 2 new server cases verified to FAIL pre-fix; frontend 65/65, server 66/66. Deployed: commit `ff36f4f`, Vercel live at 1.0.111 (fix verified over HTTPS), VM sha256 `fccf9f39…` verified identical, `/readyz` 200.

## ADR-0027 · 2026-07-10 · Two-agent bug sweep round: UI phantom-bump gate + tilt readout (client, v1.0.112) + bump tombstones, membership eviction, reconnect preservation (server)

**Context.** A two-agent adversarial code review (client state-lifecycle lens + server race/lifecycle lens) plus two field reports drove this round. **Field 1:** after connect → disconnect → reconnect with a concurrent pair connecting, the island showed "Bump detected" with no bump. Verified mechanism: both ceremony paths start a 120ms `motionTimer` poll of the raw motion snapshot BEFORE `startAt`; the sensor's latch is only cleared AT `startAt` (ADR-0024), so a pre-window handling jolt (gravity-delta ≥ 2 m/s² — just raising the phone) drives the island's MONOTONIC stage ladder to stage 4 "Bump detected — verifying tilt…", which can never regress; concurrent cohorts widen the pre-window gap via stagger. Evidence was never affected — UI-only. Telltale signature: headline jumps 1→4 skipping "Exchanging ultrasonic waves…". **Field 2:** users can't tell how far to tilt → tilt_not_detected feels random. **Agent findings (verified):** (S-critical) every teardown path destroyed telemetry and THEN re-evaluated survivors, so failing one crossed cohort deleted exactly the bumps vetoing the other — the ADR-0026 teardown-retry made the ADR-0025 wrong-pair match DETERMINISTIC; (S-med) `removeClient` was the one lifecycle transition without a match re-evaluation hook (deferred pairs stalled to their fail deadline); (S-med) re-joining while a member of a started session created ghost dual membership (double capacity count, spurious late failed, cohort-wide deferral on never-coming telemetry); (S-med) same-clientId reconnect purged session membership → device ran the whole ceremony then bounced `session_not_available`; (S-low) cancel left nonces/acousticCapabilities/matched (departed 44.1kHz phone kept capping the cohort band); (S-low) pruned duplicate-device tabs got no notification; (C-high) island-cancel → instant re-Connect overlapped two ceremonies on the singleton sensors and the OLD ceremony's `finally` stopped the NEW attempt's motion listener + mic capture mid-window ("retry right after cancel always fails"); (C-low) ceremony-exception fallback reported the un-reset pre-window latch as bump/tilt evidence; (C-low) `suppressDisconnectToast` boolean could leak across sessions when the peer:disconnected echo was lost.

**Decision.** **Client (v1.0.112):** motionTimer polls are gated on `Date.now() >= startAt` in both paths (the island never sees the pre-window latch); live tilt readout `[data-island-ceremony-tilt]` during stages 3-4 fed by the same polls — `MotionProximitySensor.getSnapshot()` now exposes `tiltThresholdDeg`, island shows "Tilt: X° — tilt past Y°" flipping to green "Tilt detected ✓" (en/ja), hidden outside stages 3-4/on zero samples/from stage 5; ceremony generation counter — each attempt increments, island-cancel and signaling-disconnect also increment and island-cancel stops sensors immediately, each ceremony's `finally` stops the singleton sensors only if its generation is still current; exception fallback zeroes motion evidence when thrown before `startAt`; `suppressDisconnectToastUntil` deadline (3s) replaces the boolean. **Server:** `proximityBumpTombstones` (15s TTL, cap 256) — every telemetry-destroying path (fail teardown, cancel, disconnect, re-join eviction) records unmatched members' bump times first and `foreignProximityBumpTimes` folds unexpired tombstones into the veto; shared `evictProximitySessionMember` (tombstone + clear all five per-member structures + teardown-when-empty) used by cancel/removeClient/join-eviction; `removeClient` re-runs `tryMatchStartedProximitySessions` when it mutated a session; `joinProximitySession` evicts the sender from a started/expired prior session before admitting them to a new cohort; same-clientId `registerClient` replacement passes `preserveProximitySessions: true` (membership is keyed by that id and stays valid across an in-page socket swap); pruned duplicate-device clients receive `proximity:session:failed { reason: "replaced_by_same_device" }`.

**Consequences.** "Bump detected" can no longer appear without an in-window bump; users get live tilt feedback; cancel-then-retry no longer self-sabotages; the crossed-cohort wrong-pair hole is closed against teardown timing; disconnects unblock deferred matches immediately; retries stop ghost-inflating capacity and stalling other cohorts; in-page WS blips no longer evict a mid-ceremony device. Deferred (logged, not fixed): unsolicited `proximity:session:joined/start` events can overwrite `proximitySessionId` mid-attempt (low, needs waiter-owned assignment). Tests: server 68/68 (tombstone + ghost-eviction regressions verified to FAIL pre-fix), frontend 65/65, app-ui e2e green except two pre-existing environment failures (audio-hardware loopback, WebKit file-open) reproduced on the unmodified tree.

## ADR-0028 · 2026-07-10 · Session-scoped waiters + de-synced cohort bump cues + veto hygiene (client v1.0.114 + server)

**Context.** Second agent round: an architecture review of the controller/hub structure plus a deep-research pass on making simultaneous multi-pair pairing reliable (full report in the session; key artifacts summarized here). **Architecture findings (verified in code):** (F1-HIGH) the proximity waiter slots were nulled by handlers UNCONDITIONALLY while the resolvers' sessionId filters rejected without consuming — a stale session's late `start`/`match`/`ack` frame (cancel → fast retry race) destroyed the new attempt's waiter, which then hung its full timeout into `proximityErrorSync` ("works on retry" family); (F2-HIGH) `proximity:session:failed` was not session-scoped and resolved all waiters with an explicit null — bypassing their guards — so any late failed frame from a previous session (lingering failTimer, `replaced_by_same_device`) aborted the current attempt and overwrote the user-visible failure reason; (F3-HIGH, one site fixed) `beginActiveConnection`'s 30s-late failure continuation stopped the shared sensors with no generation check. **Research findings:** the system MANUFACTURED same-beat multi-pair collisions — "Bump now" fires at startAt and concurrent cohorts were staggered only 300/600ms, inside the 700ms veto margin; decode correlation is amplitude-normalized (`normalizedCorrelation` divides out energy), so "loudest decode" is really "cleanest decode" and cannot see distance — explaining the touching-partner 0.102 vs 2m-phone 0.447 field datum; a latent false-veto bug: veto loops counted telemetry entries whose bump never fired, whose "bump time" is a fallback timestamp (client `Date.now()` at telemetry build / server `receivedAt`); `bumpAt` was overwritten on every above-threshold 60Hz sample, adding ~100ms jitter.

**Decision.** **Client (v1.0.114):** waiter resolvers return true only when the frame belongs to their session and handlers clear the slot only on consumption (a foreign frame leaves the waiter armed); `proximity:session:failed` is ignored when it names a different session than the current attempt's (null-sessionId pre-join failures still apply); `beginActiveConnection`'s failure continuation checks `ceremonyGeneration` before stopping sensors; `bumpAt` stamps the FIRST threshold crossing. **Server:** `DEFAULT_ACOUSTIC_SESSION_STAGGER_MS` 300 → 1200 so adjacent cohorts' bump cues land outside the veto margin (failTimer math already includes staggerMs); veto power requires real bump evidence — `hasCloserBumpPartner`'s in-session loop, `foreignProximityBumpTimes`, and `recordProximityBumpTombstones` all skip entries without `analysis.physicalEvidence.bump`. Veto regression tests updated to `bumpedInsufficientMetrics()` (real bump+tilt, no acoustic — the actual field geometry); new regression proves a bump-less entry cannot veto a genuine sloppy-delta pair.

**Consequences.** Cancel-retry races stop eating the next attempt's server frames; stale sessions can no longer abort or mislabel the current attempt; two pairs tapping together but landing in different cohorts now bump ≥1.2s apart by construction — the already-working different-moments matching handles them; arbitrary fallback timestamps can no longer veto genuine pairs; pair-matching bump timestamps lose the multi-sample jitter. Tests: server 69/69 (false-veto regression verified to FAIL pre-fix), frontend 65/65, e2e at baseline. **Roadmap accepted from the research (not yet implemented):** Step 1.4 instrumentation (per-slot RMS/peak/clipFraction + own-chirp offset for BeepBeep time-of-flight shadow ranging + motion waveform ring buffer), Step 2 per-device solo bump cues in >2 cohorts, Step 3 cohort merge for simultaneous tappers, Step 4 promote ToF distance to a matcher gate. Architecture roadmap: R1 attempt-object (`ProximityAttempt`), R2 version-bump script + consistency gate, R3 server session-owned lifecycle, R4 extract admin monitor, R5 pure stage reducer + controller harness test. The UI-doctor audit was cut short by a session limit and should be re-run.

## ADR-0029 · 2026-07-11 · Guided tour presents as a bottom sheet; new WebDrop brand icon asset (v1.0.117)

**Context.** The v1.0.116 tour rendered as a centered modal card, breaking the app's sheet-first design language; the welcome slide showed the old `webdrop-mark.svg` with free-floating avatars that didn't resemble the actual orbit UI; the bump animation ended abruptly and silently; the send scene (file dropping onto tiles) read poorly; and finishing the tour had a visible stagger (abrupt `hidden` after the swipe). The user also supplied a new brand icon (`webdrop-icon.avif`, 1024px, alpha).

**Decision.** (1) The tour is a **bottom sheet**: composer surface tokens, sheet radii/motion (`--motion-sheet-in/out`, animated backdrop), `sheet-handle`, full-width bottom-anchored on ≤759px — open forces a style flush between un-hiding and `data-open` so the slide-up transition runs; close plays the outro before `hidden`. (2) The canonical raster brand icon is **`assets/icons/webdrop-icon.webp`** (512px WebP with alpha, derived from the `assets/icons/webdrop-icon.avif` source via sips→cwebp; macOS sips can decode but not encode webp). The favicon/`webdrop-mark.svg` are untouched for now — the webp is used where a rich raster icon is wanted (tour final slide) and is SW-precached. (3) Tour scenes mirror the real UI: mini orbit (dashed drifting rings + centered self + counter-rotated orbiters, same transform trick as `.peer-node`), sender/receiver file cards with the tiles-as-progress bar racing 0→100%, QR with the owner's avatar centered. (4) Scene sound is synthesized in-gesture (no assets): the bump slide plays a thud+bloop at the CSS collision moment — JS constants (`BUMP_CYCLE_MS`, 32% contact) mirror `onboarding.css` keyframes and the scene's animations are rewound to phase 0 when the slide activates; the AudioContext is unlocked on FAB tap because scroll is not a reliable user activation.

**Consequences.** The tour now reads as one of the app's sheets and the exit is a single motion (chime → 420ms read → slide-down). Anyone editing the bump keyframes must update the JS timing constants (and vice versa) — they are deliberately documented at both sites. The webp join the precache list; future icon refreshes should re-derive from the avif source. `.claude/launch.json` carries a second static-server config (port 4186) so parallel sessions can preview without fighting over 4180.
